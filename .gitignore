# Trainer-only material is kept outside this repository, in ../python-accelerated-trainer/.
# Unlike the newer modules, which ship trainer/ inside the repo, this one cannot: its
# labs are scored, and the speaker notes carry the Read-the-PR answers.
CLAUDE.md
trainer/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
build/
dist/

# Deck-check scratch files
presentation/.probe-*.html
presentation/.canary*.html
presentation/.h.html

# Coverage, if anyone runs it over the solutions
.coverage
.coverage.*
htmlcov/

# Editors and OS
.vscode/
.idea/
.DS_Store
Thumbs.db
