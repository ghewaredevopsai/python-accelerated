"""The JSON API, mounted under /api.

Mission 3: build it spec-first from docs/api-spec.md, in Copilot agent mode.
The tests in tests/test_m3_api.py are the acceptance criteria.
"""
from fastapi import APIRouter

router = APIRouter()
