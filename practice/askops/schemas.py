"""Pydantic models - the shapes the API accepts and returns.

Mission 3: write these from docs/api-spec.md. Keep them separate from askops/models.py:
the dataclasses are the domain, these are the wire contract, and the two change for different
reasons.
"""
from pydantic import BaseModel  # noqa: F401  (you will need it)
